# (https://youtu.be/4XTsAAHW_Tc?t=33603)
(https://youtu.be/4XTsAAHW_Tc?t=35049)
# different types of error
    1.Compile time (syntax error)
    2.Run time it can be called exceptions
    3.Logical error(in common Bugs)

# Unchecked exception is not compulsory but checked exception is compulsory

# throw is use to throw custom exception and then catch block will catch it
    can call the catch block using throw keyword

# throws throws is a keyword in Java that is used in the signature of a method
    to indicate that this method might throw one of the listed type exceptions.
    The caller to these methods has to handle the exception using a try-catch block.

# e.printStackTrace prints the stack of the method show the calling
# ducking the exception

# finally will be execute irrespective of the exception

# Runtime exception is unchecked(Not compulsory to handle) exception
    SQL exception and IOException etc is called checked (Compulsory handling needed)
       exception